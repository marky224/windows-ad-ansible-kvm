---
# Ansible Galaxy collections required for the AD lab.
# Install: ansible-galaxy collection install -r requirements.yml
#
# Versions are PINNED to the known-good set validated on real hardware (M1-M7), so both
# local installs and CI are deterministic: a new upstream release can never silently
# change behavior or break the ansible-lint syntax-check gate. Bump deliberately, then
# re-validate on hardware. (Same discipline as the pinned virtio-win 0.1.271, ADR-018.)

collections:
  # AD provisioning + management. v1.11.0 added cs_authority + cs_template (AD CS partly native).
  - name: microsoft.ad
    version: "1.11.0"

  # Generic Windows modules (win_powershell, win_command, win_copy, win_reboot, etc.).
  - name: ansible.windows
    version: "2.2.0"

  # Extra community-maintained Windows modules (DNS partial, registry helpers, etc.).
  - name: community.windows
    version: "2.1.0"

  # libvirt domain / network / storage management for the KVM host.
  - name: community.libvirt
    version: "1.3.0"

  # firewalld, mount, sysctl, etc. for the Ubuntu control node and UBUNTU01-corp.
  - name: ansible.posix
    version: "1.5.4"

  # General-purpose modules (archive, ini_file, password generation, etc.).
  - name: community.general
    version: "8.3.0"

  # VyOS network automation for the Phase 6 inter-site router (ADR-052).
  # network_cli over SSH; vyos_config + vyos_static_routes (DHCP relay via set-commands).
  - name: vyos.vyos
    version: "6.0.0"
